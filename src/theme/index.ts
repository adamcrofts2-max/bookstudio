export * from '@/types/theme'

// Phase 4 will add: theme resolvers, CSS variable generation per theme,
// and the built-in theme presets themselves (Premium Nature, Classic
// Novel, Coffee Table, Educational, Children's, Minimal). This module is
// the single place the rest of the app should import theme data from,
// so swapping the implementation later never requires touching callers.
