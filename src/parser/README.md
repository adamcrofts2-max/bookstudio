# parser

DOCX / Markdown / TXT / HTML manuscript parsing into the `Content` layer
(`src/types/content.ts`). Built in **Phase 2**. Must never assume
anything about the active theme or layout — parsing produces pure
content only.
